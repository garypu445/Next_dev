import { Metadata } from "next";
import LoginPage from './page';

export const metadata: Metadata = {
	
	title: {
		absolute: "Login"
	},
	description: "Login Suotoo Member.登入索圖食品的會員"
}

export default LoginPage;