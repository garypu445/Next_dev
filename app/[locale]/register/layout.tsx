import { Metadata } from "next";
import RegisterPage from './page';

export const metadata: Metadata = {
	
	title: {
		absolute: "Register"
	},
	description: "Register Suotoo Member.註冊索圖食品的會員"
}

export default RegisterPage;